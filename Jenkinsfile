pipeline {
    agent any

        stage('Build') {
            steps {
                echo 'Building the project... (simulated)'
            }
        }

        stage('End') {
            steps {
                echo 'Pipeline finished successfully.'
            }
        }
    }
}
